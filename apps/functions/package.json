{
  "name": "functions",
  "description": "Firebase Functions package, auto generated by @simondotm/nx-firebase",
  "scripts": {
    "lint": "nx lint functions",
    "build": "nx build functions",
    "serve": "npm run build && firebase emulators:start --only functions --config firebase.functions.json",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions:simonFunctions2 --config firebase.functions.json",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": " 16"
  },
  "dependencies": {},
  "devDependencies": {},
  "private": true
}
