{
  "name": "simon-app-1",
  "description": "Firebase Functions package, auto generated by @simondotm/nx-firebase",
  "scripts": {
    "lint": "nx lint simon-app-1",
    "build": "nx build simon-app-1",
    "serve": "npm run build && firebase emulators:start --only functions --config firebase.json",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions --config firebase.json",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": " 16"
  },
  "dependencies": {},
  "devDependencies": {},
  "private": true
}
